#import <UIKit/UIKit.h>

// Release settings UI now owns its own branding, report button, developer button,
// and version display directly inside YTNicoSettingsViewController.m.
// Keep this file as a no-op so older Makefile entries continue to build without
// injecting duplicate controls.

%ctor {
}
